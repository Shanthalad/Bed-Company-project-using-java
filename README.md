## ⚙ How the Project Works
1. The `BedCompanyRespository` class stores bed brand names in an array.
2. Brands are added using the `saveBrand()` method.
3. The `findAll()` method displays all stored brands.
4. The `find()` method checks whether a specific brand exists.
5. The `BedCompany` class contains the `main()` method to run the program.


## 🚀 Features
- Store bed company brand names
- Display all stored brands
- Search for a specific brand
- Prevents storing data when array is full
- Beginner-friendly and easy to understand
